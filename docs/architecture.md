# Architecture

Placeholder — full architecture document to be written.

## Components

- **Extract:** AWS Lambda (`lambda/spotify_extractor.py`)
- **Load:** S3 + Snowflake Snowpipe (`snowflake/`)
- **Transform:** dbt (`dbt/`)
- **Orchestrate:** Airflow (`airflow/`)
- **Visualize:** Streamlit (`streamlit/`)

## Diagram

_TBD_
